<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="portal.login.mapper.RegisterMapper">

	<select id="checkUserData" resultType="int">
		select count(*) 
		from user
		where
			email = #{email} 
		and 
			password = #{password}
	</select>
	
	<insert id="insertUserInfo">
		insert into user
		(
			email, 
			signup_date, 
			password
		)
		values 
		(
		  	#{email}, 
		  	date_format(now(), '%Y%m%d'),
		  	#{password}
		 )
	</insert>
	
</mapper>